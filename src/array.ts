export {};

let nums: number[] = [1, 2, 3];

console.log(nums);

let numbers: Array<number> = new Array(12, 24);

console.log(numbers);

let strs: Array<string> = ['tokyo', 'osaka'];
console.log(strs);

let arr: (number | boolean | string)[] = [1, false, 'a'];
console.log(arr);
